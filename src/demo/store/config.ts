import cuisines from './cuisines';
import courses from './courses';
import miniBrowse from './miniBrowse';
import ratings from './ratings';
import recipe from './recipe';
import { cuisineCountConfig, courseCountConfig, ratingCountConfig, tagCountConfig } from './browse';
import search from './search';
import tags from './tags';

const config = [
  cuisineCountConfig,
  cuisines,
  courseCountConfig,
  courses,
  miniBrowse,
  ratingCountConfig,
  ratings,
  recipe,
  search,
  tagCountConfig,
  tags,
];

export default config;
